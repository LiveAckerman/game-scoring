---
description: Document generation should not be performed manually unless explicitly requested by the user.
globs: **/*
alwaysApply: true
---
# 文档生成规则

## 核心原则

**禁止主动生成文档，除非用户明确要求**

## 具体规则

### ❌ 绝对禁止的行为

1. **不要主动创建文档文件**
   - 不要创建 README.md（除非用户明确要求）
   - 不要创建 API_GUIDE.md、INTEGRATION.md 等文档
   - 不要创建 QUICK_START.md、TUTORIAL.md 等教程文档
   - 不要创建 CHANGELOG.md、TODO.md 等项目管理文档
   - 不要创建任何 `.md` 文件（除非用户明确要求）

2. **不要在代码中添加过多注释**
   - 避免明显的、冗余的注释（如 "// 导入模块"）
   - 避免在每个函数都添加大段说明注释
   - 只在真正需要解释非显而易见逻辑时添加注释

3. **不要"帮助"用户写文档**
   - 不要说"我已经为你创建了文档"
   - 不要说"我写了一个详细的指南"
   - 不要主动整理成文档格式

### ✅ 允许的行为

1. **代码本身**
   - 编写清晰的代码实现
   - 添加必要的 JSDoc/TypeScript 类型注释
   - 在代码中保留关键的业务逻辑注释

2. **回答问题**
   - 在聊天回复中解释功能
   - 回答用户的具体问题
   - 提供代码示例

3. **仅当用户明确要求时**
   - 用户说"创建文档"、"写一个 README"
   - 用户说"生成 API 文档"
   - 用户明确要求任何形式的文档

## 判断标准

### 用户明确要求（可以生成文档）：
- "创建一个 README"
- "写一份 API 文档"
- "帮我生成使用说明"
- "需要一个集成指南"
- "写个 QUICK_START"

### 用户未明确要求（禁止生成文档）：
- "完成这个功能"（只实现代码）
- "帮我集成 API"（只写代码，不写文档）
- "添加音乐生成"（只写功能代码）
- 用户没有提到"文档"、"说明"、"指南"等词

## 示例

### ❌ 错误做法

```
用户: "完成音乐生成功能"
AI:
1. 创建了功能代码
2. 创建了 AI_MUSIC_INTEGRATION.md ❌（用户没要求）
3. 创建了 QUICK_START.md ❌（用户没要求）
4. 在代码里加了大量注释 ❌（过度）
```

### ✅ 正确做法

```
用户: "完成音乐生成功能"
AI:
1. 创建了功能代码 ✅
2. 添加了必要的类型注释 ✅
3. 在回复中简单说明使用方法 ✅
4. 不创建任何 .md 文档 ✅
```

## 例外情况

以下文件除外（这些是项目必需的）：
- `README.md`（如果项目初始就有）
- `package.json` 中的 description 字段
- 代码中的 JSDoc 类型注释
- 环境变量示例（`env.example`）

## 总结

**核心思想：代码说话，少写文档，除非用户明确要求。**
